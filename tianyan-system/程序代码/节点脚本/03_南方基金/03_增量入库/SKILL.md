# 南方基金事务入库

节点 ID：`southern_load`

该节点自 2026-08-16 起不进入每日 DAG，仅供手工南方专项更新使用。只有同批 `southern_gate` 通过后才能运行。节点持有 `main_db_write` 资源锁，加载器通过 `SOUTHERN_COLLECT_RUN_ID` 精确选择单一完整批次，并在一个 SQLite 事务内替换南方渠道数据。加载事务内必须完成目录与业绩水位核对，失败即回滚；加载后继续反查策略数、最新业绩日、当前仓位、历史仓位和基准成分。未形成合格新批次时保留上一成功版本；每日启用渠道的后续加工、稽核和自动发布不依赖本节点。
