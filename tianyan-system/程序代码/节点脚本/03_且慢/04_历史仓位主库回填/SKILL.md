# 且慢历史仓位主库回填

节点 ID：`qieman_history_backfill`

仅用于把最近一次已通过且慢 gate 的 `strategy_fund_snapshot_history` 完整快照事务回填到主库 `策略历史持仓`。节点持有 `main_db_write` 锁，不调用且慢网络接口，不改变当前持仓、调仓、业绩或信号表。

验收要求：标准化输入行数与主库落地行数一致；不存在策略缺失或业务键缺失跳过；至少存在一个权重闭合的完整历史快照。失败时事务整体回滚。
