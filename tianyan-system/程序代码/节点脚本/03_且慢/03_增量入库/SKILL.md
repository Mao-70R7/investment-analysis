# 且慢事务入库

节点 ID：`qieman_load`

仅接收同批次 gate 通过的完整标准化快照，持有 `main_db_write` 资源锁后按 `qieman` 渠道事务替换。事务内核验目录 ID 和新增策略 ID；失败即回滚并保留旧渠道数据。

普通组合的官方调仓进入现有调仓表；官方完整历史仓位快照进入独立的 `策略历史持仓` 事实表。发车买卖指令仍进入信号表，只有官方完整调前/调后仓位才进入历史持仓，绝不把新增资金分配比例伪装成存量仓位。
