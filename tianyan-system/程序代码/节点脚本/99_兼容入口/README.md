# 兼容入口

本目录仅保留目录重构前的入口名称一轮。旧 BAT 和 PowerShell 入口会打印废弃提示，并转发根目录 `00_每日数据更新并发布_唯一入口.bat`；它们不再承载业务逻辑。

- `历史实现`：旧入口名称兼容层。
- `运行工作区模板`：旧迁移包模板，仅用于历史排查，不作为当前初始化入口。
- `兼容转发.bat`、`兼容转发.ps1`：统一解析开发/迁移布局并调用正式入口。

新功能不得依赖本目录。连续两次生产运行成功后，可单独评估删除兼容层。
