# 节点化调度与目录重构验收

验收日期：2026-07-23

## 实施结果

- 根目录正式入口收敛为 `00_每日数据更新并发布_唯一入口.bat`，并保留 `AGENTS.md`、`README_AI.md`。
- 每日链路拆为 19 个正式节点，生产和运维节点共 29 个；每个节点均有 `SKILL.md`、`node.json`、`run.ps1`、`src` 和 `tests`。
- 已实现节点依赖校验、资源锁、进程超时、重试、原子结果、输入输出指纹、文件哈希、业务水位、失败续跑和下游失效。
- 节点状态写入 `daily_update_node`、`daily_update_node_attempt` 和 `daily_update_artifact`，不改变主业务数据库表结构。
- 控制台、文本日志和结构化事件日志同步记录总进度、节点进度、对象计数、耗时、设备和重试。
- 旧入口已迁入 `99_兼容入口`；旧 BAT 和 PowerShell 仅提示废弃并转发正式入口。
- 研究和开发辅助入口已归入 `98_非生产工具`，不进入每日 DAG。

## 验收证据

- 开发工程单元测试：47 项通过。
- 迁移运行工作区单元测试：47 项通过。
- 生产路径扫描：270 个文件，固定盘符、旧 `scripts/` 生产引用和项目根推断问题为 0。
- 开发工程 DAG dry-run：19/19 成功，运行批次 `20260723T111842+0800`。
- 迁移运行工作区最终 DAG dry-run：19/19 成功，运行批次 `20260723T121911+0800`。
- 迁移工作区最终初始化：成功，运行批次 `20260723T120910+0800`；632 个文件校验通过，读取 19.55 GiB，并建立恢复后的成功基线备份。
- 迁移工作区最终检查：3/3 节点成功，运行批次 `20260723T121538+0800`。
- MuMu 自动启动并通过 ADB、天天 App、缓存访问及登录证据检查。
- GitHub 发布仓库远端、推送 dry-run 和 Pages `version.json` 均可访问。
- 项目数据稽核：静态错误 0、数据错误 0、数据警告 4，最终摘要见 `outputs/data_audit_hook/2026-07-23/20260723T122036+0800/hook_summary.json`，明细见 `outputs/data_audit/2026-07-23/20260723T124055+0800/data_audit_report.json`。

## 当前保留警告

- 实体手机在本次验收时未连接，MuMu 可用；每日运行仍可执行，但设备级备用能力需在新服务器连接实体手机后再次确认。
- 数据稽核保留 4 项业务警告：7 条已披露基准未完成真实映射、160 条正权重持仓缺区间同类排名、74 只基金详情净值点不足、1 只成熟 FOF 明确缺历史且 9 只成立日期待确认。
- 业务质量另有页面大数据包和重要策略元数据缺失率 5.78% 两项警告；均为本次目录重构前已存在的问题，未新增数据错误。
- 本次没有执行会真实采集、写库、备份并推送 GitHub 的完整每日运行；已完成初始化、数据库健康检查和 19 节点 dry-run。下一次正常调度运行应作为首个生产验收批次。

## 迁移工作区恢复记录

- 清理 `D:\SyncthingShare\天眼系统` 的重构前归档时，归档内遗留的目录联接被递归删除命令跟随，导致迁移工作区数据库文件被误删；开发工程数据库未受影响。
- 迁移主库和辅助库已从开发工程中经 `quick_check` 验证通过的数据库，使用 SQLite Backup API 重新生成一致性副本；主库恢复后再次验证为 `quick_check=ok`、`user_version=20260712`、65 张表。
- 恢复后主库关键行数为：策略 1,275 条、策略标准业绩净值 1,321,753 条、当前持仓 62,247 条、调仓事件 10,408 条、基金 26,862 条、基金日度净值 33,162,424 条。
- 迁移工作区运行状态库已独立重建，因此恢复前的节点状态不作为续跑依据；历史文本日志仍保留。恢复后环境与数据库检查批次 `20260723T120438+0800` 为 3/3 成功。
- 恢复后的数据库摘要和哈希已写回 `迁移基线/migration_manifest.json` 与 `迁移基线/checksums.sha256`；最终再次核验 632 个文件、19.55 GiB，错误数为 0。
- Windows 资源锁的进程存活检查已从 `tasklist` 文本匹配改为内核 `OpenProcess`，修复活动锁可能被错误回收的问题；开发工程和迁移工作区各 47 项测试均通过。

## 后续验收条件

1. 新服务器连接实体手机并再次运行 `initialize` 或设备预检，确认 MuMu 和真机均可用。
2. 完成一次真实每日更新，核对数据水位、节点缺口、成功备份、Git 提交和 Pages `buildId`。
3. 在无新数据或少量新数据场景再运行一次，验证幂等和续跑。
4. 连续两次生产运行成功后，再单独评估删除 `99_兼容入口`。
